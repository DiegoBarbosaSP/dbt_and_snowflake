version: 2

models:
  - name: stg_payments
    description: payments from customer data from our jaffle shop app.
    columns: 
      - name: payment_id
        description: the payment id key from orders.
        tests:
          - unique
          - not_null
      - name: order_id
        description: '{{doc("payment_status")}}'
        

